apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: amq-broker-dca-service-monitor
spec:
  endpoints:
    - interval: 30s
      port: console-jolokia
      scheme: http
  selector:
    matchLabels:
      application: {{ dc_a_artemis_cluster }}-app